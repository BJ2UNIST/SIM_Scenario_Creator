import * as maptalks from "maptalks";

import { calcAngle } from "./lineutils";

function NodeManager(nodeLayer, junctionLayer, laneManager) {
  function loadNodes(features: any): void {}

  return {
    loadNodes,
  };
}

export default NodeManager;
